apply plugin: 'com.bmuschko.nexus'

nexus {
    sign = false
    repositoryUrl = project.publishRepositoryUrl
    snapshotRepositoryUrl = project.publishSnapshotRepositoryUrl
}
extraArchive {
    sources = true
    tests = false
    javadoc = false
}